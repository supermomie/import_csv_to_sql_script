# README


### Install requirement

```bash
pip install -r requirements.txt

```


```bash

python import_db_to_sql.py  ? [csv/json]

                            Mode d'emploi :                                

PATH/(getcwd) [-d="directory"] [-tn="table name"] [-cn="column name"] [-a]?

Arguments : 

  -d  / --dir         : Absolute path of file/folder (string) 
  -tn / --table_name  : Database Name (string) 
  -cn / --column_name : Column name (string) 
  -a  / --all         : All compatible files in folder (string) 

```
